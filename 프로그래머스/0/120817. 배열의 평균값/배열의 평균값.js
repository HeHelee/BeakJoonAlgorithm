function solution(numbers) {
    let answer = 0;
    // 구하는 것 : 원소의 평균값을 구하기
    // 배열의 값을 다 더해야 함.
    // 더한 값을 개수만큼 나눠줘야 함.
    let sum = 0;
    for (number of numbers) {
        sum += number;
    }
    answer = sum / numbers.length;
    
    return answer;
}